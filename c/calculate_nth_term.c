#include <stdio.h>

int find_nth_term(int n, int a, int b, int c) {

}

int main() {
    int n;
    int a, b, c;

    scanf("%d", &n);
    scanf("%d%d%d", &a, &b, &c);

    int nth = find_nth_term(n, a, b, c);
}